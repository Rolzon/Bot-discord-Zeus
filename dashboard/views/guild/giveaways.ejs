<%- include('../layout', { title: 'Sorteos - ' + guild.name, body: `
<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h1 class="h3 mb-1">
            <i class="fas fa-gift me-2"></i>
            Gestión de Sorteos
        </h1>
        <p class="text-muted mb-0">${guild.name}</p>
    </div>
    <a href="/guild/${guild.id}" class="btn btn-outline-secondary">
        <i class="fas fa-arrow-left me-2"></i>Volver
    </a>
</div>

<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="fas fa-calendar-check me-2"></i>
                    Sorteos Activos
                </h5>
            </div>
            <div class="card-body">
                <div class="alert alert-info">
                    <i class="fas fa-info-circle me-2"></i>
                    Panel de sorteos en desarrollo. Próximamente podrás crear y gestionar sorteos desde aquí.
                </div>
                
                <h6 class="mb-3">Funciones próximamente:</h6>
                <ul>
                    <li>Crear nuevos sorteos</li>
                    <li>Ver sorteos activos y finalizados</li>
                    <li>Gestionar participantes</li>
                    <li>Seleccionar ganadores manualmente</li>
                    <li>Estadísticas de sorteos</li>
                </ul>
            </div>
        </div>
    </div>
</div>
`, script: `` }) %>
