<%- include('../layout', { title: 'Configuración - ' + guild.name, body: `
<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h1 class="h3 mb-1">
            <i class="fas fa-cog me-2"></i>
            Configuración del Servidor
        </h1>
        <p class="text-muted mb-0">${guild.name}</p>
    </div>
    <a href="/guild/${guild.id}" class="btn btn-outline-secondary">
        <i class="fas fa-arrow-left me-2"></i>Volver
    </a>
</div>

<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="fas fa-wrench me-2"></i>
                    Configuración General
                </h5>
            </div>
            <div class="card-body">
                <div class="alert alert-info">
                    <i class="fas fa-info-circle me-2"></i>
                    Panel de configuración en desarrollo. Próximamente podrás personalizar todos los ajustes del bot.
                </div>
                
                <h6 class="mb-3">Funciones próximamente:</h6>
                <ul>
                    <li>Configurar prefijos y comandos</li>
                    <li>Ajustar permisos del bot</li>
                    <li>Configurar canales de logs</li>
                    <li>Personalizar mensajes de bienvenida</li>
                    <li>Configuración de módulos (música, tickets, etc.)</li>
                </ul>
            </div>
        </div>
    </div>
</div>
`, script: `` }) %>
