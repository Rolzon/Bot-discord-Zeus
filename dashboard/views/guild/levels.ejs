<%- include('../layout', { title: 'Niveles - ' + guild.name, body: `
<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h1 class="h3 mb-1">
            <i class="fas fa-trophy me-2"></i>
            Sistema de Niveles
        </h1>
        <p class="text-muted mb-0">${guild.name}</p>
    </div>
    <a href="/guild/${guild.id}" class="btn btn-outline-secondary">
        <i class="fas fa-arrow-left me-2"></i>Volver
    </a>
</div>

<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="fas fa-chart-line me-2"></i>
                    Gestión de Niveles y XP
                </h5>
            </div>
            <div class="card-body">
                <div class="alert alert-info">
                    <i class="fas fa-info-circle me-2"></i>
                    Panel de niveles en desarrollo. Próximamente podrás gestionar el sistema de XP desde aquí.
                </div>
                
                <h6 class="mb-3">Funciones próximamente:</h6>
                <ul>
                    <li>Ver tabla de clasificación (leaderboard)</li>
                    <li>Gestionar XP de usuarios</li>
                    <li>Configurar recompensas por nivel</li>
                    <li>Roles automáticos por nivel</li>
                    <li>Estadísticas y gráficos de progresión</li>
                </ul>
            </div>
        </div>
    </div>
</div>
`, script: `` }) %>
