<%- include('../layout', { title: 'TempVoice - ' + guild.name, body: `
<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h1 class="h3 mb-1">
            <i class="fas fa-microphone me-2"></i>
            TempVoice
        </h1>
        <p class="text-muted mb-0">${guild.name}</p>
    </div>
    <a href="/guild/${guild.id}" class="btn btn-outline-secondary">
        <i class="fas fa-arrow-left me-2"></i>Volver
    </a>
</div>

<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="fas fa-sliders-h me-2"></i>
                    Configuración de Canales Temporales
                </h5>
            </div>
            <div class="card-body">
                <div class="alert alert-info">
                    <i class="fas fa-info-circle me-2"></i>
                    Panel de TempVoice en desarrollo. Próximamente podrás configurar canales de voz temporales desde aquí.
                </div>
                
                <h6 class="mb-3">Funciones próximamente:</h6>
                <ul>
                    <li>Configurar canal generador</li>
                    <li>Personalizar permisos predeterminados</li>
                    <li>Ver canales temporales activos</li>
                    <li>Gestionar canales manualmente</li>
                    <li>Estadísticas de uso</li>
                </ul>
            </div>
        </div>
    </div>
</div>
`, script: `` }) %>
