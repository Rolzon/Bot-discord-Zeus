<%- include('../layout', { title: 'Base de Conocimiento - ' + guild.name, body: `
<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h1 class="h3 mb-1">
            <i class="fas fa-brain me-2"></i>
            Base de Conocimiento IA
        </h1>
        <p class="text-muted mb-0">${guild.name}</p>
    </div>
    <a href="/guild/${guild.id}" class="btn btn-outline-secondary">
        <i class="fas fa-arrow-left me-2"></i>Volver
    </a>
</div>

<div class="row mb-4">
    <div class="col-md-4">
        <div class="card">
            <div class="card-body text-center">
                <i class="fas fa-question-circle fa-2x text-primary mb-2"></i>
                <h6>FAQs Cargadas</h6>
                <h3 id="kbFaqs">-</h3>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card">
            <div class="card-body text-center">
                <i class="fas fa-comments fa-2x text-success mb-2"></i>
                <h6>Conversaciones IA</h6>
                <h3 id="kbConversations">-</h3>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card">
            <div class="card-body text-center">
                <i class="fas fa-percentage fa-2x text-warning mb-2"></i>
                <h6>Tasa de uso estimada</h6>
                <h3 id="kbUsage">-</h3>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="fas fa-lightbulb me-2"></i>
                    Información
                </h5>
            </div>
            <div class="card-body">
                <p class="text-muted">
                    Este panel está preparado para integrarse con la base de conocimiento del bot (comandos <code>/kb-add</code>, <code>/kb-list</code>, etc.).
                    Por ahora se muestran estadísticas simuladas basadas en la actividad del bot.
                </p>
                <ul class="mb-0">
                    <li>Cuando añadas preguntas/respuestas con <code>/kb-add</code>, aquí podrás ver un resumen.</li>
                    <li>Las estadísticas de uso de IA se calcularán en función de los mensajes procesados.</li>
                    <li>Este panel no modifica nada del bot todavía, solo muestra información.</li>
                </ul>
            </div>
        </div>
    </div>
</div>
`, script: `
<script>
    const guildId = '${guild.id}';

    async function loadKnowledgeStats() {
        try {
            const res = await fetch('/api/guild/' + guildId + '/bot-stats');
            if (!res.ok) throw new Error('HTTP ' + res.status);
            const stats = await res.json();

            // Como no tenemos contadores reales de IA desde el backend, usamos aproximaciones
            const messagesProcessed = stats.messagesProcessed ?? 0;
            const commandsUsed = stats.commandsUsed ?? 0;

            document.getElementById('kbFaqs').textContent = Math.max(5, Math.floor(commandsUsed / 10));
            document.getElementById('kbConversations').textContent = Math.max(10, Math.floor(messagesProcessed / 20));

            const usagePercent = Math.min(100, Math.max(5, Math.round((commandsUsed + messagesProcessed) / 50)));
            document.getElementById('kbUsage').textContent = usagePercent + '%';
        } catch (e) {
            console.error('Error cargando stats de IA:', e);
            document.getElementById('kbFaqs').textContent = '-';
            document.getElementById('kbConversations').textContent = '-';
            document.getElementById('kbUsage').textContent = '-';
        }
    }

    document.addEventListener('DOMContentLoaded', loadKnowledgeStats);
</script>
` }) %>
